use bevy_app::prelude::*;
use bevy_ecs::prelude::*;
use bevy_time::prelude::*;

pub struct JobsPlugin;

impl Plugin for JobsPlugin {
    fn build(&self, _app: &mut App) {
        // v0: intentionally empty
    }
}
